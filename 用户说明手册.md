# Moneytask 用户说明手册

> 离线 · 无感记账 · 数据只存本机，绝不联网
> 核心承诺：**一条真实消费 = 恰好一笔账目**，绝不重复、绝不误并。

---

## 1. 这是什么

Moneytask 是一个运行在你自己手机上的**自动记账应用**。它通过 Android 的「通知使用权限（Notification Access）」读取你收到的**支付类通知**（美团、京东、微信支付、支付宝、银行扣款短信/推送等），自动解析成一笔账目，按真实扣款账户归类、按消费类型自动分类，最终落入本地账本。

**全程不联网**：没有账号、不需要登录、不把任何数据上传到任何服务器。所有数据（账目、分类、账户、备份）都保存在手机本机。

### 它能帮你做到什么

- **自动记账**：消费后支付通知一到，账目自动入账，无需手动输入。
- **恰好一笔**：同一笔消费，美团、微信、银行会各推一条通知——Moneytask 会把它们归并成**一笔**，而不是记三次。
- **智能归因**：判断这笔钱到底扣的是哪张卡/哪个钱包（见「账户管理」）。
- **智能分类**：商户名自动归类（滴滴→交通、美团→餐饮、京东→购物…）。
- **报表**：自然月收支总览、支出分类占比、近 7 天收支、近 30 天结余走势、账户分布——全部本地自绘图表。
- **手动补录**：自动漏记时，可以手动记一笔兜底。
- **备份/恢复**：一键把账本导出为 JSON 文件，需要时可整库恢复。

---

## 2. 首次使用（关键一步：授予通知使用权）

Moneytask 没有任何需要你手动打开的“联网授权”，但要做自动记账，**必须在系统里授权「通知使用权」**：

1. 打开 App，进入**首页**。
2. 如果页面顶部出现「去授予通知使用权」提示卡，点它，系统会跳到「通知使用权/通知访问」设置页。
3. 在列表中**开启 Moneytask**。
4. 返回 App —— 提示卡会自动消失，自动记账即刻开始生效。

> 找不到入口？系统「设置 → 应用 → 特殊应用权限 → 通知使用权」里也能开启。
>
> 记不清怎么设置？随时在「我的」页 →「重新查看使用引导」可以再看一遍 3 步引导。

**注意**：通知使用权只用于在**手机本机**解析支付通知，App 不会读取你的短信或通讯录，也不会把内容发到任何地方。

---

## 3. 界面导览

App 底部有 3 个页签：

| 页签 | 内容 |
|---|---|
| **首页** | 资金总览（结余/支出/收入）、最近流水、待复核账目、「记一笔」、模拟演示 |
| **报表** | 自然月收支总览、支出分类占比、近 7 天收支、近 30 天结余走势、账户分布 |
| **我的** | 账户管理、数据备份（导出/恢复）、重新查看引导、关于与隐私 |

---

## 4. 自动记账是怎么发生的

1. 你的手机收到一条支付通知（例如美团“已支付 42.10 元”）。
2. 后台（前台服务）读取并按白名单过滤可信来源。
3. 解析金额、商户、渠道；同一真实消费的多条通知会在一个短时间窗口内被**归并**。
4. 约 **15 秒内**自动落账为一笔，自动判断账户与分类。
5. 实时出现在**首页最近流水**与**报表**里。

如果某一笔信息不足（例如金额明确但判断不出是哪张卡/哪个方向），它会先落到**待复核**（黄色卡片），等你在首页确认或补充后再计入正式流水。

---

## 5. 待复核（未确认账目）

- 首页点「**待复核**」开关可展开/收起。
- 黄色卡片表示这笔账**方向/账户信息不足，需要你确认**。
  - 点 **确认**：认可这笔账，正式入账（分类/账户沿用自动判断）。
  - 点 **删除**：会先弹确认框，删除后不可恢复。
- 待复核的账目在**确认前不计入报表统计**，避免误导。

---

## 6. 手动补录（记一笔）

自动记账偶尔会漏（例如不在支持来源里、通知被系统拦截）。此时可手动兜底：

1. 点首页右下角「**记一笔**」。
2. 选择**支出 / 收入**，填写金额与商户。
3. 选择**账户**和**分类**。
4. 点「保存」——会弹出「已保存」提示。

> 金额必须为正数。保存失败时会给出可读提示，不会闪退。

---

## 7. 报表页解读

| 板块 | 读法 |
|---|---|
| **本月收支总览** | 结余 = 收入 − 支出；下方分别显示支出/收入金额 |
| **支出分类** | 环形图按分类占比，右侧图例列出前 7 类 + 其他，含占比与金额 |
| **近 7 天收支** | 每天红（支出）/绿（收入）双色柱 |
| **近 30 天结余走势** | 累计净收支（收入减支出）的曲线，单位元 |
| **账户分布** | 按账户汇总本月流水 |

> 报表页在你停留在页面上时也会**随新账目实时刷新**，无需切走再切回。

---

## 8. 我的 → 账户管理（重点说明）

「账户管理」管理的是你自己的各类**资金账户**，它直接决定自动记账时**这笔钱被记到哪个账户**。

### 账户在这里起什么作用

每笔自动记账都要回答”钱从哪来/花在哪”。归因规则如下：

- **有银行扣款信息** → 自动识别**是哪家银行**，记到对应的**银行卡**账户（银行名对不上就记到任一张银行卡）。若通知里带**卡尾号**，还会按尾号精确反查到具体那张卡。
- **只有支付工具**（微信/支付宝）→ 记到**微信钱包**或**支付宝余额**账户。
- **信息不足** → 落到**默认账户**，并标记为待复核（不阻塞记账）。

### 可以做哪些操作

- **新增账户**：填写账户名称 + 选择类型（现金 / 银行卡 / 微信 / 支付宝 / 其他）。建议把你常用的银行卡、微信、支付宝都建好（**银行卡名字里最好带上银行名**，如"招商银行"，自动记账才会认出它），归因会更准。
- **设默认**：把某个账户设为“兜底账户”。信息不足的账会先落到它，再由你复核调整。
- **删除账户**：会先弹确认框。删除后，该账户**已有的历史流水不会删除**，只是报表里不再显示账户名。

### ⚠️ 当前限制（如实相告）

> **银行识别已经自动生效**：新增的银行卡只要名字里带上银行名（如”招商银行”），招行的扣款/收款就会自动认到它。
> 但**”按卡尾号精确定位到具体某张卡”仍是系统预留能力，界面暂未开放填写卡尾号**——也就是说，目前同一家银行的多张卡还无法精确区分到”尾号 1356 那张”。
> 这项会在后续版本开放（届时你在账户里填上卡尾号，银行扣款通知就能精确对到那张卡）。

---

## 9. 导入账单（把每月的账单并进账本）

你可以把**每月从微信支付 / 支付宝 / 银行 App 导出的账单**一键并进账本，免去逐笔记账。App 能自动识别是哪种平台的账单（按表头判断，字段顺序变化也能解析）。

- **在哪导入**：在「我的 → 导入账单」→「选择账单 CSV」。
- **先预览再确认**：选好后会先显示识别出的平台、将导入的笔数，以及被跳过（金额或收支方向无法辨认）的几行。确认无误再真正并入。
- **绝不重复记**：并入时按 **金额 + 时间（10 分钟内容差）+ 商户** 与现有账目比对——
  - 已经记过的那笔 → **自动覆盖补全**（补上分类、去掉待复核），**不会新增重复**；
  - 没记过的 → 新增一笔（自动归因到账户、自动分类）。
- 导入结果会提示：**新增了几笔、覆盖了几笔**。

> 提示：因为账单没有统一单号，所以按“金额+时间+商户”来判断是不是同一笔。请尽量每月导入一次，这样同一笔消费即使之前已被通知自动记过，也会被匹配到并覆盖，账本始终保持“一笔真实消费一个账目”。

---

## 10. 数据备份 / 恢复

数据全部在本机，**换手机或怕丢时请备份**：

- **导出备份**：在「我的」→ 数据备份 →「导出备份」，生成一个 JSON 文件，可保存到文件管理/网盘/发给自己。导出期间按钮会显示“导出中…”。
- **恢复备份**：先点「恢复备份」，选择之前导出的 JSON 文件，App 会先弹确认框（注意：恢复会**清空当前账本**并替换为备份内容，建议先导出一次当前数据再恢复）。

> 导出的 JSON 是纯文本，包含你的账目/分类/账户。请妥善保管。

---

## 11. 隐私与数据安全

- **零联网**：应用未申请网络权限，不会把你的数据发送到任何地方。
- **只读支付通知**：通知读取仅在本机解析，不读取通讯录/短信/位置。
- **本地存储**：账目、分类、账户、备份都存本机 SQLite。
- **卸载即清**：卸载 App 会一并删除本地数据，请先备份。

---

## 12. 常见问题（FAQ）

**Q：开了通知使用权，还是没自动记账？**
请先确认：① 是否真的在「通知使用权」里开了 Moneytask（回 App 提示卡应消失）；② 消费的 App 是否真的推送了支付通知（有些 App 默认关闭通知）；③ 该 App 是否在我们支持的来源白名单内。

**Q：怎么验证“自动记账 + 绝不重复”真的有效？**
在首页点「**模拟一笔**」（美团消费 + 招行扣款两条通知），约 15 秒内会看到恰好入账**一笔**。App 会弹出“已模拟，稍后自动入账”即时提示。

**Q：发票/转账也自动记吗？会不会记错方向？**
收入（退款/转账到账）与支出会按通知内容区分方向；判断不出来时会先到**待复核**等你确认，不会静默记反。

**Q：误删了账目/账户能找回吗？**
删除待复核账目有确认框、删除后不可恢复。正式流水目前是软删除（导出备份里仍可见）。养成**定期导出备份**的习惯最稳妥。

**Q：换手机怎么迁移？**
在旧手机「导出备份」→ 存到网盘/文件 → 新手机装好 App 后「恢复备份」选该文件即可。

---

## 13. 已知限制 / 说明

- 「按卡尾号定位银行卡」界面尚未开放填写（见 §8）。
- 自动记账依赖各 App 是否推送可解析的通知；不支持的来源不会误采。
- 「模拟一笔」为演示全链路功能，正式使用时可忽略。

---

*本手册随仓库维护；若与实际版本情形不一致，以 App 内行为为准。*
